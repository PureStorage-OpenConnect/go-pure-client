# \RealmConnectionsAPI

All URIs are relative to *http://localhost*

Method | HTTP request | Description
------------- | ------------- | -------------
[**RealmConnectionsConnectionKeysDelete**](RealmConnectionsAPI.md#RealmConnectionsConnectionKeysDelete) | **Delete** /api/2.52/realm-connections/connection-keys | Delete realm connection keys
[**RealmConnectionsConnectionKeysGet**](RealmConnectionsAPI.md#RealmConnectionsConnectionKeysGet) | **Get** /api/2.52/realm-connections/connection-keys | Get realm connection key information
[**RealmConnectionsConnectionKeysPost**](RealmConnectionsAPI.md#RealmConnectionsConnectionKeysPost) | **Post** /api/2.52/realm-connections/connection-keys | Create realm connection key
[**RealmConnectionsDelete**](RealmConnectionsAPI.md#RealmConnectionsDelete) | **Delete** /api/2.52/realm-connections | Delete realm connections.
[**RealmConnectionsGet**](RealmConnectionsAPI.md#RealmConnectionsGet) | **Get** /api/2.52/realm-connections | List realm connections.
[**RealmConnectionsPatch**](RealmConnectionsAPI.md#RealmConnectionsPatch) | **Patch** /api/2.52/realm-connections | Modify realm connections.
[**RealmConnectionsPost**](RealmConnectionsAPI.md#RealmConnectionsPost) | **Post** /api/2.52/realm-connections | Create realm connection.



## RealmConnectionsConnectionKeysDelete

> RealmConnectionsConnectionKeysDelete(ctx).Authorization(authorization).XRequestID(xRequestID).ContextNames(contextNames).RealmIds(realmIds).RealmNames(realmNames).Execute()

Delete realm connection keys



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsConnectionKeysDelete(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsConnectionKeysDelete``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsConnectionKeysDeleteRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **contextNames** | **[]string** | Performs the operation on the context specified.  If specified, the context names must be an array of size 1, and the single element must be the name of an array in the same fleet or the name of the fleet itself. If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  | 
 **realmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The realm_ids and realm_names parameters cannot be set together.  | 
 **realmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The realm_names and realm_ids parameters cannot be set together.  | 

### Return type

 (empty response body)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: Not defined

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## RealmConnectionsConnectionKeysGet

> RealmConnectionKeyGetResponse RealmConnectionsConnectionKeysGet(ctx).Authorization(authorization).XRequestID(xRequestID).AllowErrors(allowErrors).ContextNames(contextNames).ContinuationToken(continuationToken).Filter(filter).Limit(limit).Offset(offset).RealmIds(realmIds).RealmNames(realmNames).Sort(sort).TotalItemCount(totalItemCount).Execute()

Get realm connection key information



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsConnectionKeysGet(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsConnectionKeysGet``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
    // response from `RealmConnectionsConnectionKeysGet`: RealmConnectionKeyGetResponse
    fmt.Fprintf(os.Stdout, "Response from `RealmConnectionsAPI.RealmConnectionsConnectionKeysGet`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsConnectionKeysGetRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **allowErrors** | **bool** | If set to &#x60;true&#x60;, the API will allow the operation to continue even if there are errors. Any errors will be returned in the &#x60;errors&#x60; field of the response. If set to &#x60;false&#x60;, the operation will fail if there are any errors.  | [default to false]
 **contextNames** | **[]string** | Performs the operation on the context specified.  If specified, the context names must be an array of size 1, and the single element must be the name of an array in the same fleet or the name of the fleet itself. If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  | 
 **continuationToken** | **string** | A token used to retrieve the next page of data with some consistency guaranteed. The token is a Base64 encoded value. Set &#x60;continuation_token&#x60; to the system-generated token taken from the &#x60;x-next-token&#x60; header field of the response. A query has reached its last page when the response does not include a token. Pagination requires the &#x60;limit&#x60; and &#x60;continuation_token&#x60; query parameters.  | 
 **filter** | **string** | Narrows down the results to only the response objects that satisfy the filter criteria.  | 
 **limit** | **int32** | Limits the size of the response to the specified number of objects on each page. To return the total number of resources, set &#x60;limit&#x3D;0&#x60;. The total number of resources is returned as a &#x60;total_item_count&#x60; value. If the page size requested is larger than the system maximum limit, the server returns the maximum limit, disregarding the requested page size.  | 
 **offset** | **int32** | The starting position based on the results of the query in relation to the full set of response objects returned.  | 
 **realmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The realm_ids and realm_names parameters cannot be set together.  | 
 **realmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The realm_names and realm_ids parameters cannot be set together.  | 
 **sort** | **[]string** | Sorts the response objects by the specified fields. Sorting can be applied to any field name  in the response, in ascending order by default, or in descending order by prefixing the  field name with a minus sign (-). Multiple fields can be specified as a comma-separated  list (e.g., sort volumes by size descending, then by name ascending). If sort is provided,  the response will not include a continuation_token.  | 
 **totalItemCount** | **bool** | If set to &#x60;true&#x60;, the &#x60;total_item_count&#x60; matching the specified query parameters is calculated and returned in the response. If set to &#x60;false&#x60;, the &#x60;total_item_count&#x60; is &#x60;null&#x60; in the response. This may speed up queries where the &#x60;total_item_count&#x60; is large. If not specified, defaults to &#x60;false&#x60;.  | 

### Return type

[**RealmConnectionKeyGetResponse**](RealmConnectionKeyGetResponse.md)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## RealmConnectionsConnectionKeysPost

> RealmConnectionKeyPostResponse RealmConnectionsConnectionKeysPost(ctx).ConnectionKey(connectionKey).Authorization(authorization).XRequestID(xRequestID).ContextNames(contextNames).RealmIds(realmIds).RealmNames(realmNames).Execute()

Create realm connection key



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsConnectionKeysPost(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsConnectionKeysPost``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
    // response from `RealmConnectionsConnectionKeysPost`: RealmConnectionKeyPostResponse
    fmt.Fprintf(os.Stdout, "Response from `RealmConnectionsAPI.RealmConnectionsConnectionKeysPost`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsConnectionKeysPostRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **connectionKey** | [**RealmConnectionKeyPost**](RealmConnectionKeyPost.md) |  | 
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **contextNames** | **[]string** | Performs the operation on the context specified.  If specified, the context names must be an array of size 1, and the single element must be the name of an array in the same fleet or the name of the fleet itself. If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  | 
 **realmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The realm_ids and realm_names parameters cannot be set together.  | 
 **realmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The realm_names and realm_ids parameters cannot be set together.  | 

### Return type

[**RealmConnectionKeyPostResponse**](RealmConnectionKeyPostResponse.md)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: application/json
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## RealmConnectionsDelete

> RealmConnectionsDelete(ctx).Authorization(authorization).XRequestID(xRequestID).ContextNames(contextNames).Ids(ids).LocalRealmIds(localRealmIds).LocalRealmNames(localRealmNames).RemoteRealmIds(remoteRealmIds).RemoteRealmNames(remoteRealmNames).Execute()

Delete realm connections.



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsDelete(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsDelete``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsDeleteRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **contextNames** | **[]string** | Performs the operation on the context specified.  If specified, the context names must be an array of size 1, and the single element must be the name of an array in the same fleet or the name of the fleet itself. If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  | 
 **ids** | **[]string** | Performs the operation on the unique resource IDs specified. Enter multiple resource IDs in comma-separated format.  | 
 **localRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The local_realm_ids and local_realm_names parameters cannot be set together.  | 
 **localRealmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The local_realm_names and local_realm_ids parameters cannot be set together.  | 
 **remoteRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 
 **remoteRealmNames** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 

### Return type

 (empty response body)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: Not defined

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## RealmConnectionsGet

> RealmConnectionsGetResponse RealmConnectionsGet(ctx).Authorization(authorization).XRequestID(xRequestID).AllowErrors(allowErrors).ContextNames(contextNames).ContinuationToken(continuationToken).Filter(filter).Ids(ids).Limit(limit).LocalRealmIds(localRealmIds).LocalRealmNames(localRealmNames).Offset(offset).RemoteRealmIds(remoteRealmIds).RemoteRealmNames(remoteRealmNames).Sort(sort).TotalItemCount(totalItemCount).Execute()

List realm connections.



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsGet(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsGet``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
    // response from `RealmConnectionsGet`: RealmConnectionsGetResponse
    fmt.Fprintf(os.Stdout, "Response from `RealmConnectionsAPI.RealmConnectionsGet`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsGetRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **allowErrors** | **bool** | If set to &#x60;true&#x60;, the API will allow the operation to continue even if there are errors. Any errors will be returned in the &#x60;errors&#x60; field of the response. If set to &#x60;false&#x60;, the operation will fail if there are any errors.  | [default to false]
 **contextNames** | **[]string** | Performs the operation on the unique contexts specified.  If specified, each context name must be the name of an array in the same fleet or the name of the fleet itself.  If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  Enter multiple names in comma-separated format. For example, &#x60;name01,name02&#x60;.  | 
 **continuationToken** | **string** | A token used to retrieve the next page of data with some consistency guaranteed. The token is a Base64 encoded value. Set &#x60;continuation_token&#x60; to the system-generated token taken from the &#x60;x-next-token&#x60; header field of the response. A query has reached its last page when the response does not include a token. Pagination requires the &#x60;limit&#x60; and &#x60;continuation_token&#x60; query parameters.  | 
 **filter** | **string** | Narrows down the results to only the response objects that satisfy the filter criteria.  | 
 **ids** | **[]string** | Performs the operation on the unique resource IDs specified. Enter multiple resource IDs in comma-separated format.  | 
 **limit** | **int32** | Limits the size of the response to the specified number of objects on each page. To return the total number of resources, set &#x60;limit&#x3D;0&#x60;. The total number of resources is returned as a &#x60;total_item_count&#x60; value. If the page size requested is larger than the system maximum limit, the server returns the maximum limit, disregarding the requested page size.  | 
 **localRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The local_realm_ids and local_realm_names parameters cannot be set together.  | 
 **localRealmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The local_realm_names and local_realm_ids parameters cannot be set together.  | 
 **offset** | **int32** | The starting position based on the results of the query in relation to the full set of response objects returned.  | 
 **remoteRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 
 **remoteRealmNames** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 
 **sort** | **[]string** | Sorts the response objects by the specified fields. Sorting can be applied to any field name  in the response, in ascending order by default, or in descending order by prefixing the  field name with a minus sign (-). Multiple fields can be specified as a comma-separated  list (e.g., sort volumes by size descending, then by name ascending). If sort is provided,  the response will not include a continuation_token.  | 
 **totalItemCount** | **bool** | If set to &#x60;true&#x60;, the &#x60;total_item_count&#x60; matching the specified query parameters is calculated and returned in the response. If set to &#x60;false&#x60;, the &#x60;total_item_count&#x60; is &#x60;null&#x60; in the response. This may speed up queries where the &#x60;total_item_count&#x60; is large. If not specified, defaults to &#x60;false&#x60;.  | 

### Return type

[**RealmConnectionsGetResponse**](RealmConnectionsGetResponse.md)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## RealmConnectionsPatch

> RealmConnectionsResponse RealmConnectionsPatch(ctx).Connection(connection).Authorization(authorization).XRequestID(xRequestID).ContextNames(contextNames).Ids(ids).LocalRealmIds(localRealmIds).LocalRealmNames(localRealmNames).RemoteRealmIds(remoteRealmIds).RemoteRealmNames(remoteRealmNames).Execute()

Modify realm connections.



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsPatch(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsPatch``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
    // response from `RealmConnectionsPatch`: RealmConnectionsResponse
    fmt.Fprintf(os.Stdout, "Response from `RealmConnectionsAPI.RealmConnectionsPatch`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsPatchRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **connection** | [**RealmConnectionWithKey**](RealmConnectionWithKey.md) |  | 
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **contextNames** | **[]string** | Performs the operation on the context specified.  If specified, the context names must be an array of size 1, and the single element must be the name of an array in the same fleet or the name of the fleet itself. If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  | 
 **ids** | **[]string** | Performs the operation on the unique resource IDs specified. Enter multiple resource IDs in comma-separated format.  | 
 **localRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The local_realm_ids and local_realm_names parameters cannot be set together.  | 
 **localRealmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The local_realm_names and local_realm_ids parameters cannot be set together.  | 
 **remoteRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 
 **remoteRealmNames** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 

### Return type

[**RealmConnectionsResponse**](RealmConnectionsResponse.md)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: application/json
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## RealmConnectionsPost

> RealmConnectionsResponse RealmConnectionsPost(ctx).Connection(connection).Authorization(authorization).XRequestID(xRequestID).ContextNames(contextNames).LocalRealmIds(localRealmIds).LocalRealmNames(localRealmNames).RemoteRealmIds(remoteRealmIds).RemoteRealmNames(remoteRealmNames).Execute()

Create realm connection.



### Example

```go
package main

import (
    "context"
    "fmt"
    "os"
    openapiclient "github.com/pure-shared/go-pure-client/flasharray/FA_2_52"
)

func main() {
    configuration, err := openapiclient.NewConfigurationBuilder(server.URL).
        APIToken(apiToken).
        Build()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client configuration: %v\n", err)
    }
    apiClient, err := openapiclient.NewClient(configuration)
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when creating api client: %v\n", err)
    }
    req := apiClient.RealmConnectionsAPI.RealmConnectionsPost(context.Background())
    // Set request parameters ...

    resp, r, err := req.Execute()
    if err != nil {
        fmt.Fprintf(os.Stderr, "Error when calling `RealmConnectionsAPI.RealmConnectionsPost``: %v\n", err)
        fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
    }
    // response from `RealmConnectionsPost`: RealmConnectionsResponse
    fmt.Fprintf(os.Stdout, "Response from `RealmConnectionsAPI.RealmConnectionsPost`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiRealmConnectionsPostRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **connection** | [**RealmConnectionWithKey**](RealmConnectionWithKey.md) |  | 
 **authorization** | **string** | Access token (in JWT format) required to use any API endpoint (except &#x60;/oauth2&#x60;, &#x60;/login&#x60;, and &#x60;/logout&#x60;)  | 
 **xRequestID** | **string** | Supplied by client during request or generated by server.  | 
 **contextNames** | **[]string** | Performs the operation on the context specified.  If specified, the context names must be an array of size 1, and the single element must be the name of an array in the same fleet or the name of the fleet itself. If not specified, the context will default to the array that received this request.  Other parameters provided with the request, such as names of volumes or snapshots, are resolved relative to the provided &#x60;context&#x60;.  | 
 **localRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The local_realm_ids and local_realm_names parameters cannot be set together.  | 
 **localRealmNames** | **[]string** | Performs the operation on the specified unique resource names. Enter multiple resource names in comma-separated format. The local_realm_names and local_realm_ids parameters cannot be set together.  | 
 **remoteRealmIds** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 
 **remoteRealmNames** | **[]string** | Performs the operation on the specified unique resource IDs. Enter multiple resource IDs in comma-separated format. The remote_realm_ids and remote_realm_names parameters cannot be set together.  | 

### Return type

[**RealmConnectionsResponse**](RealmConnectionsResponse.md)

### Authorization

Authorization is handled by Authorizer specified in configuration and data are filled automatically. Use param authorization only when you need to override this behaviour.

### HTTP request headers

- **Content-Type**: application/json
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)

